[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npx --yes pnpm@11.9.0 install --frozen-lockfile"]

[phases.build]
cmds = ["npx --yes pnpm@11.9.0 turbo run build --filter=@nogal/api..."]

[start]
cmd = "node apps/api/dist/main.js"
